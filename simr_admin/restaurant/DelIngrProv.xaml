<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="restaurant.DelIngrProv"
             x:Name="ThisPage"
             Title="Manage Ingredient Providers">
    <Grid>
        <VerticalStackLayout Padding="10">
            <CollectionView ItemsSource="{Binding Providers}" SelectionMode="Single">
                <CollectionView.EmptyView>
                    <Label Text="No providers found."
                           VerticalOptions="CenterAndExpand"
                           HorizontalOptions="CenterAndExpand" />
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <SwipeView IsVisible="{OnPlatform Android=true, iOS=true, WinUI=false, MacCatalyst=false}">
                                <SwipeView.RightItems>
                                    <SwipeItems Mode="Reveal">
                                        <SwipeItem Text="Delete"
                                               BackgroundColor="LightSalmon"
                                               Command="{Binding Path=BindingContext.DeleteCommand, Source={x:Reference ThisPage}}"
                                               CommandParameter="{Binding .}" />
                                        <SwipeItem Text="Edit"
                                               BackgroundColor="Orange"
                                               Command="{Binding Path=BindingContext.EditCommand, Source={x:Reference ThisPage}}"
                                               CommandParameter="{Binding .}" />
                                    </SwipeItems>
                                </SwipeView.RightItems>
                                <Frame Padding="15"
                                       Margin="0,5"
                                       BorderColor="#DDD"
                                       BackgroundColor="#FAFAFA"
                                       CornerRadius="12"
                                       HasShadow="True">
                                    <Label Text="{Binding}" 
                                           FontSize="18" 
                                           VerticalOptions="Center"
                                           TextColor="Black" />
                                </Frame>
                            </SwipeView>
                            <Frame IsVisible="{OnPlatform Android=false, iOS=false, WinUI=true, MacCatalyst=true}"
                                   Padding="15"
                                   Margin="0,5"
                                   BorderColor="#DDD"
                                   BackgroundColor="#FAFAFA"
                                   CornerRadius="12"
                                   HasShadow="True">
                                <VerticalStackLayout Spacing="8">
                                    <Label Text="{Binding}"
                                           FontSize="18"
                                           TextColor="Black" />
                                    <HorizontalStackLayout Spacing="10" Margin="0,10,0,0">
                                        <Button Text="Edit"
                                                BackgroundColor="Orange"
                                                TextColor="White"
                                                Command="{Binding Path=BindingContext.EditCommand, Source={x:Reference ThisPage}}"
                                                CommandParameter="{Binding .}" />
                                        <Button Text="Delete"
                                                BackgroundColor="LightSalmon"
                                                TextColor="White"
                                                Command="{Binding Path=BindingContext.DeleteCommand, Source={x:Reference ThisPage}}"
                                                CommandParameter="{Binding .}" />
                                    </HorizontalStackLayout>
                                </VerticalStackLayout>
                            </Frame>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
        <Button Text="+" 
                Command="{Binding AddIngrProv}"
                FontSize="28"
                WidthRequest="60"
                HeightRequest="60"
                CornerRadius="30"
                BackgroundColor="LightSkyBlue"
                TextColor="White"
                HorizontalOptions="End"
                VerticalOptions="End"
                Margin="20"/>
    </Grid>
</ContentPage>
