<ContentPage
    x:Class="restaurant.DelMenuIngr"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:restaurant.ViewModels"
    x:Name="ThisPage"
    Title="Manage Menu Ingredients">

    <Grid>
        <RefreshView Command="{Binding RefreshCommand}" IsRefreshing="{Binding IsRefreshing}">
            <CollectionView ItemsSource="{Binding Ingredients}" SelectionMode="None">
                <CollectionView.EmptyView>
                    <Label Text="No ingredients found."
                           HorizontalOptions="Center"
                           VerticalOptions="Center" />
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <SwipeView IsVisible="{OnPlatform Android=true, iOS=true, WinUI=false, MacCatalyst=false}">
                                <SwipeView.RightItems>
                                    <SwipeItems Mode="Reveal">
                                        <SwipeItem
                                            Text="Delete"
                                            BackgroundColor="LightSalmon"
                                            Command="{Binding Path=BindingContext.DeleteIngredientCommand, Source={x:Reference ThisPage}}"
                                            CommandParameter="{Binding Key}" />
                                        <SwipeItem
                                            Text="Edit"
                                            BackgroundColor="Orange"
                                            Command="{Binding Path=BindingContext.EditIngredientCommand, Source={x:Reference ThisPage}}"
                                            CommandParameter="{Binding .}" />
                                    </SwipeItems>
                                </SwipeView.RightItems>
                                <Frame Padding="15"
                                       Margin="0,5"
                                       BorderColor="#DDD"
                                       BackgroundColor="#FAFAFA"
                                       CornerRadius="12"
                                       HasShadow="True">
                                    <Label Text="{Binding Value.name}"
                                           FontSize="18"
                                           VerticalOptions="Center"
                                           TextColor="Black" />
                                </Frame>
                            </SwipeView>

                            <Frame IsVisible="{OnPlatform Android=false, iOS=false, WinUI=true, MacCatalyst=true}"
                                   Padding="15"
                                   Margin="0,5"
                                   BorderColor="#DDD"
                                   BackgroundColor="#FAFAFA"
                                   CornerRadius="12"
                                   HasShadow="True">
                                <VerticalStackLayout Spacing="8">
                                    <Label Text="{Binding Value.name}"
                                           FontSize="18"
                                           TextColor="Black" />
                                    <HorizontalStackLayout Spacing="10">
                                        <Button Text="Edit"
                                                BackgroundColor="Orange"
                                                TextColor="White"
                                                Command="{Binding Path=BindingContext.EditIngredientCommand, Source={x:Reference ThisPage}}"
                                                CommandParameter="{Binding .}" />
                                        <Button Text="Delete"
                                                BackgroundColor="LightSalmon"
                                                TextColor="White"
                                                Command="{Binding Path=BindingContext.DeleteIngredientCommand, Source={x:Reference ThisPage}}"
                                                CommandParameter="{Binding Key}" />
                                    </HorizontalStackLayout>
                                </VerticalStackLayout>
                            </Frame>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
        <Button Text="+" 
                Command="{Binding AddMenuIngr}"
                FontSize="28"
                WidthRequest="60"
                HeightRequest="60"
                CornerRadius="30"
                BackgroundColor="LightSkyBlue"
                TextColor="White"
                HorizontalOptions="End"
                VerticalOptions="End"
                Margin="20"/>
    </Grid>
</ContentPage>
